"""上下文组装与窗口维护 (ADR-0041).

对外两个入口, 职责不重叠:

- ``ContextAssembler`` —— 这一轮的上下文长什么样 (六层怎么取材).
- ``WindowManager`` —— 窗口该不该淘汰.

合成一个会让"为什么这一轮变贵了"再也说不清是哪一半干的.

包门面不转导出任何东西 (见 scripts/check_arch.py 的环检查).
"""
