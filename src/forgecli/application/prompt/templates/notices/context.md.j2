{#
上下文淘汰与归档取回 (ADR-0041).

原先这里还有四条占位, 供去重与降级**顶替** transcript 里的工具结果正文. 那两条通路已随
ADR-0041 决策 4 删除 —— 窗口只追加, 而正文本来就不进窗口了, 没有可顶替的东西.

剩下的分两类: artifact_read 取不回来时的回执, 以及窗口淘汰时的那几句.
#}
{#
artifact_read 取不到时回给模型的结论.
#}
{% macro artifact_missing() %}
该输出已过期回收, 取不回来了. 还需要这份内容的话重新执行一次原来的调用.
{% endmacro %}
{% macro artifact_bad_id() %}
artifact_id 只能是 16 位十六进制字符, 不能包含路径.
{% endmacro %}
{% macro artifact_window_truncated() %}
[这一段仍然超出单次回填上限, 已截断; 用 offset 与 limit 取更小的一段]
{% endmacro %}
{#
二级摘要 (决策 2). 给摘要模型的指令, 以及摘要在 transcript 里的包装.
#}
{% macro compaction_instruction() %}
把下面这段对话压缩成一份交接说明, 供你自己在上下文被截断之后继续同一个任务.

必须保留: 用户的目标与明确约束; 已经做完的关键动作及其结果; 改过或正在关注的文件;
失败, 风险与还没做完的事; 下一步打算.

不要保留: 逐字的文件内容, 完整的命令输出, 已经不影响后续判断的中间过程.

只输出这份说明本身, 不要有前言后语.
{% endmacro %}
{#
送进摘要请求的历史被截断时的抬头. 明说截掉的是**前面**那一段, 否则摘要模型会以为这就是
对话的开头, 从而把"任务是从这里开始的"写进交接说明.
#}
{% macro compaction_input_clipped() %}
[以下只是这段历史的后半部分, 更早的内容已因长度略去]
{% endmacro %}
{% macro compaction_header() %}
以下是本次对话早前部分的交接说明, 原文已因窗口长度被淘汰:
{% endmacro %}
{#
用户原话逐字保留 (ADR-0041 决策 5). 与摘要分开标注: 摘要是转述, 这一段是原文, 而目标与
约束读错一个字就会做错方向.
#}
{% macro compaction_user_verbatim() %}
这一段里用户说过的话, 逐字保留:
{% endmacro %}
{#
淘汰发生了, 但那一段里既没摘出交接说明, 也没有用户原话 (通常是一整段工具往返).
仍然留一条痕迹: 让模型知道前面有内容被丢掉了, 而不是以为对话本来就从这儿开始.
#}
{% macro compaction_nothing_kept() %}
(这一段没有可保留的内容)
{% endmacro %}
{% macro compaction_failed() %}
上下文超长, 且自动压缩未能完成.
{% endmacro %}
