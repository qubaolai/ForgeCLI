{#
回合中注入 transcript 的引导

它们不是系统提示词, 是回合进行中追加的 USER / TOOL 消息. 模型读它们的方式与读系统
提示词完全一样, 所以同样受改动纪律约束 —— 它们原先散在循环里, 不升版本也没有快照.
#}
{% macro halt() %}
本轮工具调用已停止: 上一次请求未获授权. 不要尝试用别的命令或工具达成同一目的 —— 那是在绕过刚才那个决定. 请说明你原本想做什么以及为什么, 然后等待用户的下一步指示.
{% endmacro %}
{% macro blocked(count) %}
本轮工具调用已停止: 已被安全策略拒绝 {{ count }} 次. 继续换写法只会继续被拒. 请说明你想达成什么, 以及被挡住的是哪一步, 然后等待用户指示.
{% endmacro %}
{% macro malformed(detail) %}
上一次回复里的工具调用无法使用: {{ detail }}. 工具调用只能走结构化的 tool_calls 字段, 不要把 <tool_call>, <arg_key>, </think> 一类标记写进正文或参数值里. 请重新给出这次调用.
{% endmacro %}
{% macro malformed_detail(tool, marker) %}
{{ tool }} 的内容里混进了 {{ marker }}
{% endmacro %}
{#
重复调用回填而不是静默跳过: 模型必须知道"你在重复", 否则它只会原样再要一次.
#}
{% macro repeat_call(call, limit) %}
重复调用已被拦截: {{ call }} 在本轮已经调用过 {{ limit }} 次, 参数完全相同, 结果不会变. 请换一个做法, 或者基于已有结果直接作答.
{% endmacro %}
{% macro barren(count) %}
刚才连续 {{ count }} 次工具调用没有带回新信息: 要么是空结果, 要么与上一次完全相同. 换个写法再试一次多半还是这个结果. 如果这几次是在找同一样东西, 那么找不到本身就是结论, 直接说出来; 否则换一条思路 —— 换个入口文件, 换个关键词, 或者问用户.
{% endmacro %}
{#
排队中的调用要补一份配对的 tool result: 协议要求 assistant 消息里每个 tool_call 都有
对应结果, 直接丢掉它们, 下一次请求就是残缺的.
#}
{% macro abandoned_call(notice) %}
未执行: {{ notice }}
{% endmacro %}
{% macro review_abandon() %}
未执行: 本轮已停下等待用户对上一步产出做决定.
{% endmacro %}
{#
会话未装配工具系统. 前缀由 ObservationKind 提供, 这里只给措辞 —— 手写一遍
"[tool_unavailable] " 就是第二份会漂的格式.
#}
{% macro tools_not_wired() %}
当前会话未装配工具系统
{% endmacro %}
{#
工作区变化由 loop 在下一次模型调用前追加. 路径是文件系统事实, 不把文件正文带进通知;
模型需要的是重新读取/确认的线索. source 要明确区分本 agent 与外部参与者.
#}
{% macro workspace_changed(changes) %}
工作区文件发生变化, 请不要继续依据变化前的内容做决定. 必要时重新读取相关文件:
{% for change in changes %}
- {{ change.source_label }} · {{ change.kind_label }} · {{ change.display_path }}
{% endfor %}
其中 "外部参与者" 表示其他 agent 或人类在本 agent 工具边界之外做的修改.
{% endmacro %}
{#
疑似被围栏拦下 (ADR-0030). 这一条要同时说清三件事, 少一件模型就会走错路:

1. **这不是"文件不存在"或"你写错了路径"** —— 否则它会去改路径, 而路径是对的.
2. **放开边界要用户做**, 模型自己做不到 (WorkspaceGrants 明写只能由用户发起).
3. **还有一条不需要放开边界的路** —— 很多构建可以改成写工作区内的目录.

不写"重试"二字: 边界没变之前重试必然再失败一次, 而那是一整轮模型调用.
#}
{% macro fence_blocked(paths) %}
[这次失败很可能是围栏拦下的, 不是路径写错了] 命令试图访问下面的位置, 而它们在本次会话
的可写边界之外:
{% for path in paths %}
- {{ path }}
{% endfor %}
放开边界只能由用户操作 (在界面上把该目录授权为可写), 你自己改不了. 所以要么请用户授权
并说明为什么需要它, 要么换一条写在工作区内的做法 (例如把缓存或输出目录指到工作区里).
在边界改变之前重跑同一条命令只会再失败一次.
{% endmacro %}
