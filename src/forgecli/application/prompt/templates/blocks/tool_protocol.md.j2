{#
调用时序与并发. 只管每一次工具往返怎么走, 不管单个工具怎么用 —— 后者住在
ToolSpec.description, 提示词里一个工具名都不出现 (ADR-0042 决策 5).

原先这一块里还有一张工具表和一节"检索顺序". 表删掉了: tool schema 已经带了 name,
description 与 parameters, 那就是模型选工具的全部依据, 提示词不再提供第二份.
检索顺序那八条全部是**两两消歧** (A 容易被误用成 B), 每条只需要一份, 已经下沉到会被
误用的那个工具自己的 description 里 —— 那里更近, 更省, 且随该工具一起被删.
#}
{# why: 目录之外的工具模型也会请求. 说清"附带的就是全部", 它才会在缺能力时如实报告而不是猜一个名字. #}
- 随本次请求附带的 tool schema 是本轮唯一可用的工具目录.
{# why: 模型会把请求当成已批准, 在等待裁决期间就按已经生效往下写. #}
- 工具请求可能被允许, 被拒绝, 或要求人类确认. 不预设审批结果.
{# why: 一轮发一个来回等结果, 在没有依赖的调用上白白多花几个往返, 而每个往返都要重发一次上下文. #}
- 几个调用之间没有依赖就在同一轮一起发, 不要一轮发一个来回等结果.
{# why: 模型会按它预期的结果继续, 而不是按实际拿到的. 失败的调用尤其容易被当成成功往下走. #}
- 工具调用之后, 按真实结果继续: 修正方案, 或者说明卡在哪里.
{# why: 用户看到的是一串工具调用在滚动, 不说目的他不知道你在干什么, 也就无法在早期打断. #}
- 一组工具调用之前, 先用一句简短的可见文字说明目的. 不输出原始思维链.
