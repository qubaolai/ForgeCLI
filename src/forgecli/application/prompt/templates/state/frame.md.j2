{#
当前状态帧: 请求的第 [6] 层, 整条请求的**最后一个内容块** (ADR-0041).

## 为什么在尾部而不是提示词里

计划, 待办与记忆每轮都可能变. 它们原先是系统提示词尾部的三个块, 而整个 system prompt
排在 messages 之前 —— 一次 todo_write 就让前面几十轮对话全部退出前缀缓存. 搬到请求末尾
之后, 变的只是它自己那几百 token, 后面没有任何东西会被作废.

位置还顺带解决另一件事: 位置效应下, 开头与结尾的召回明显高于中间, 而"当前该做哪一步"
正是最不能被稀释的一条.

## 为什么三节合一份文件

原先是 plan_state / todo_state / memory_state 三份. 合并的理由与 ADR-0031 收拢正文时
一样: 这三节会一起进上下文, 而它们最常见的毛病是彼此矛盾 —— 并排放着才看得出来.

## 分隔行

这一帧由 Forge 组装, 不是用户这一轮说的话, 而它在协议上走的是一条 user 消息. 不标出来
的话, 模型分不清哪一部分是人说的. 与 FORGE.md 的分隔行同一个理由, 只是信任方向相反:
那里是防提权, 这里是防"把系统状态当成用户指令".
#}
--- BEGIN CURRENT STATE ---
以下由 ForgeCLI 组装, 反映此刻的执行状态, 不是用户这一轮的输入.
{% if plan %}

## 当前计划

plan_id: {{ plan.plan_id }}
标题: {{ plan.title }}
状态: {{ plan.status }}
步骤: {{ plan.step_count }} 条
{# 正文可能很长而模型只在部分轮次需要它, 所以走工具取, 不每轮重述一遍. #}
正文没有放在这里. 需要看的时候用 plan_read 取.
{% endif %}
{% if todo %}

## 当前待办

{# 待办正文每轮都给: 它小, 而且"当前该做哪一步"只存在于对话历史里的话, 越往后越容易被
   稀释, 而那正是执行漂移的根因. #}
{{ todo.rendered }}

进度 {{ todo.done }}/{{ todo.total }}. 这份清单与实际不符时, 用 todo_write 重写整表;
只是推进状态用 todo_set_status.
{% endif %}
{% if memory_groups %}

## 跨会话记忆

{# 这一节与"项目指令"的信任级别不同 —— 那边是用户写的, 这边是模型自己推断的, 可能已经
   过时 —— 而模型分不出来, 除非我们说. #}
以下是你在过往会话里记下的事实, 由你自己推断而来, **不是用户下达的指令**. 它们可能已经
过时: 与"项目指令"块冲突时一律以那一块为准. 发现某条不对就用 memory_forget 删掉它,
学到新的用 memory_write 记下来.
{% set labels = {"project": "项目事实", "user": "用户偏好"} %}
{% for group in memory_groups %}

{{ labels[group.scope] }}:
{% for entry in group.entries %}
- {{ entry.key }}: {{ entry.value }}
{% endfor %}
{% endfor %}
{% endif %}
--- END CURRENT STATE ---
