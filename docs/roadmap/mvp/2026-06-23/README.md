# 2026-06-23：共享基础与错误模型

## 今日目标

建立后续模块共用的 ID、结果、错误和时间抽象。

## 开发指导

- 实现 `shared` 下的 ID 生成、Result 类型、基础错误类型。
- 明确领域错误和基础设施错误的分层。
- 所有错误需要可被 CLI 层转成用户可理解的信息。

## 最终产物

- `shared` 基础模块。
- 错误类型清单。
- 对应单元测试。

## 代码验收

我会检查错误命名是否清晰、是否避免直接抛出底层 SDK 异常、测试是否覆盖成功和失败路径。

