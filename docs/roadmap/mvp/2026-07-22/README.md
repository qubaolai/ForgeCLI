# 2026-07-22：文件与搜索工具

## 今日目标

实现只读文件工具和文本搜索工具。

## 开发指导

- `fs.read_file` 限制在 workspace 内。
- `fs.list_files` 支持忽略 `.git`、虚拟环境、缓存目录。
- `search.text` 支持基础文本搜索。

## 最终产物

- 文件读取工具。
- 文件列表工具。
- 搜索工具。
- 路径越权测试。

## 代码验收

我会重点检查 workspace 边界和错误提示，不能允许读取或写入非授权路径。

