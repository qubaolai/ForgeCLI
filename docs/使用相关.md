# 配置内容

## 环境变量

配置文件存储根目录可以通过环境变量`FORGE_CONFIG_DIR`指定, 如果未指定, 则用户主目录下的 ~/.forge;

### 日志与可观测性 (ADR-0035)

| 变量 | 取值 | 说明 |
|---|---|---|
| `FORGE_LOG_LEVEL` | `debug` / `info` / `warn` / `error` | 覆盖配置项 `logging.level`. 临时开一次 debug 不必改配置文件 |
| `FORGE_LOG_CONSOLE` | `1` | 除文件外同时写 stderr. 默认不写: CLI 的活动区独占终端, 往 stderr 打日志会把画面戳花 |
| `FORGE_LOG_DIR` | 目录路径 | 改写日志目录. 默认 `~/.forge/logs` |
| `FORGE_LOG_MAX_VALUE` | 整数 | 单个值渲染后的字符上限, `0` 表示不截断. 默认 4000 |
| `FORGE_LOG_HTTP` | `1` | 把 httpx / httpcore / uvicorn 的日志收进同一个文件, 与 Forge 自己的记录在同一条时间线上 |

## 日志

一次运行一个文件: `~/.forge/logs/forge-<时间戳>-<pid>.log`, 保留最近 20 个.
`forge-latest.log` 指向本次运行:

```bash
tail -f ~/.forge/logs/forge-latest.log
```

每行的形状是 `时间 级别 模块 [线程] 事件名 键=值 键=值`, 一条记录一行, 所以可以直接 grep.
常用的几条:

```bash
# 某一轮从头到尾
grep 'turn=turn_0007' ~/.forge/logs/forge-latest.log

# 所有非零退出的子进程
grep 'shell.exited' ~/.forge/logs/forge-latest.log | grep -v 'exit_code=0'

# 模型侧的失败与重试
grep -E 'llm\.(failed|rate_limited|auth_failed|transport_retry)' ~/.forge/logs/forge-latest.log

# 被安全策略拒掉的工具调用
grep 'pipeline.denied' ~/.forge/logs/forge-latest.log
```

`debug` 级别额外写整段提示词 (`prompt.text`), 模型回复原文 (`model.response.text`),
工具输出 (`tool.output`), 子进程 stdout/stderr (`shell.output`) 与 HTTP payload.
它们每条几千字, 所以默认级别 `info` 不写.

**日志不脱敏**: 工具入参, 命令 argv 与环境变量, 模型请求与回复原文都原样写. 唯一的例外是
API key 的值 —— 记的是它的引用名与哈希前缀 (`credential=OPENAI_API_KEY
credential_fingerprint=3f9a1c04`). 把日志贴给别人之前请自己先看一眼: 工作区里的文件内容
在里面.

## 诊断读数

终端里:

```text
/diagnostics
```

Web 控制面:

```text
GET /api/v1/diagnostics
```

两边读的是同一份读数: 日志级别与文件路径, 各阶段的耗时分位数与计数, 以及按
provider/model 的模型调用统计 (调用数, 重试, 错误分类, 延迟分桶, cache 命中).
`/diagnostics --reset` 把计数与耗时清零, 从当前时刻重新量一次 (不影响日志文件).
